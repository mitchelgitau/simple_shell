#include "main.h"
/**
 * main - check the code
 * @argc : arguments count
 * @argv : arguments array
 * 
 * Return: Always 0.
 */
int main (int argc, char **argv)
{
    cmd(argc, argv);
    return (0);
}